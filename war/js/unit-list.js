$(document).ready(function() {

	// faster with direct, no script
	// set all unit descriptions
	 /*var descs = document.getElementsByClassName("unit_desc");

	 for(var i=0; i<descs.length/2; i++) {
		 descs[i].innerHTML = unitTitles[i];
	 }*/
	 
	/* var j=0;
	 for(var i=descs.length/2; i<descs.length; i++) {
		 descs[i-descs.length/2].innerHTML = unitTitles[j];
		 j++;
	 }*/
	 
});